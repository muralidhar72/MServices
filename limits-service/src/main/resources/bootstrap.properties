spring.application.name=${springboot.app.name:limits-service}
server.port=8889

#eureka.client.service-url.default-zone=http://localhost:8761/eureka
eureka.client.service-url.defaultZone=http://${registry.host:localhost}:${registry.port:8761}/eureka/

#spring.cloud.config.uri=http://localhost:8888
spring.cloud.conf.url=http://${cloud.host:localhost}:${cloud.port:8888}
spring.profiles.active=

#spring.application.name=${springboot.app.name:spring-cloud-config-server}
##server.port=8888
#eureka.client.service-url.defaultZone=http://${registry.host:localhost}:${registry.port:8761}/eureka/
